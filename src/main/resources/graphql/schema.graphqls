type Query {
    user(id: ID): User
    userName(id: ID): UserName
    userNames : [UserName]
}

type User {
    id: ID
    address: UserAddress
    name: UserName
}

type UserAddress {
    postcode: String
    firstLine: String
}

type UserName {
    first: String
    last: String
}
